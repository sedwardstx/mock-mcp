# Story 2.3: Ticket Search & Filter Tool

## Status

Done

## Story

**As a** student's AI agent,
**I want** to search and filter tickets by their attributes,
**so that** I can find the specific incident(s) relevant to a lab or investigation.

## Acceptance Criteria

1. A `search_tickets` tool supports filtering by attributes including Azure product/service, status, severity, customer persona (Windows admin vs. Azure developer), and affected resource.
2. Filters are combinable (AND semantics) and return a bounded, paginated summary result set.
3. A search matching no tickets returns an empty result set (not an error), and invalid filter values return a clear validation message.
4. The tool exposes a clear MCP schema documenting each supported filter and its allowed values.
5. Integration tests assert deterministic search results for representative filter combinations against known sample scenarios.

## Tasks / Subtasks

- [x] Task 1: Repository search (AC: 1, 2)
  - [x] `Repository.search_tickets(persona, status, severity, azure_product, resource_id, offset, limit) -> (tickets, total)` with AND semantics over the deterministic Dataset order
- [x] Task 2: `search_tickets` tool (AC: 1, 3, 4)
  - [x] Optional filter params; validate enum filters (persona/status/severity) against allowed values → structured `invalid_request` with message
  - [x] No matches → `ok` with empty list; document allowed values in the tool description
- [x] Task 3: Tests (AC: 5)
  - [x] Unit: repository search (single + combined filter, no-match)
  - [x] Integration: search by persona, by status, combined; no-match empty; invalid filter value → invalid_request

## Dev Notes

### Filter Design [Source: prd/requirements.md] (FR4) [Source: architecture/components.md#Repository]
- Filterable attributes: `azure_product` (exact), `status` (enum), `severity` (enum), `persona` (enum), `resource_id` (membership in ticket.resource_ids). AND semantics.
- Reuse `TicketListResult`/`TicketSummary` from Story 2.2; add an optional `message` field for validation feedback.
- Enum allowed values come from `data/models.py` (`Persona`, `Status`, `Severity`).

### Error Handling [Source: architecture/error-handling-strategy.md#Business-Logic-Errors]
- Invalid enum filter value → structured `invalid_request` with allowed values; never raise.
- Empty result is `ok` with `tickets: []` (NOT an error). [Source: prd/requirements.md] (FR- empty result behavior)

### File Locations [Source: architecture/source-tree.md]
- `src/contoso_support_mcp/data/repository.py` (extend)
- `src/contoso_support_mcp/tools/tickets.py` (extend — add `search_tickets`, add `message` to result model)
- Tests: `tests/unit/test_repository.py` (extend), `tests/integration/test_ticket_tools.py` (extend)

### Previous Story Insights (2.2)
- `Repository` and ticket tools already exist; extend them (do not create parallel modules).
- Structured-result contract established — follow it exactly.

### Testing
- pytest + pytest-asyncio; in-memory MCP client. Assert deterministic filtered results against the 3 shipped samples (personas: windows_admin ×2, azure_developer ×1; products: VMs ×2, Networking ×1).
- **Run:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-23 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-23 | 1.2 | Implemented; 41/41 tests pass, ruff clean; status → Review | James (Dev) |
| 2026-07-23 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-23 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- `uv run pytest -q` — **41 passed** (+7: 3 repository search, 4 search-tool integration).
- `uv run ruff check` — clean.

### Completion Notes List

- `Repository.search_tickets(...)`: AND-semantics filter over persona/status/severity/azure_product/resource_id, preserving Dataset order; returns `(page, total)`.
- `search_tickets` tool: all filters optional; enum filters validated against allowed values → structured `invalid_request` with the allowed set; no-match → `ok` with empty list. Added `message` to `TicketListResult`; extracted a shared `_summaries()` helper (used by both list and search).
- Tool description enumerates every filter's allowed values for schema clarity.

### File List

- `src/contoso_support_mcp/data/repository.py` (modified — `search_tickets`)
- `src/contoso_support_mcp/tools/tickets.py` (modified — `search_tickets` tool, `_summaries`, `message` field)
- `tests/unit/test_repository.py` (modified)
- `tests/integration/test_ticket_tools.py` (modified)

## QA Results

### Review Date: 2026-07-23

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Well-executed. AND-semantics filtering is clean; enum validation returns a helpful `invalid_request` listing allowed values; empty match correctly stays `ok`. Extracting `_summaries()` removed duplication between list and search. Filters are enumerated in the tool description, satisfying the "schema self-documenting" AC.

### Compliance Check

- Coding Standards: ✓  | Project Structure: ✓  | Testing Strategy: ✓  | All ACs Met: ✓

### Requirements Traceability

| AC | Validated by | Result |
|----|--------------|--------|
| 1 Filter by attributes | `test_repository.py::search_single_filter` (2.3-UNIT-001) | ✓ |
| 2 Combinable + bounded | `::combined_filters` + `test_ticket_tools.py::combined` (UNIT-002, INT-004) | ✓ |
| 3 Empty=ok / invalid=structured | `::no_match` + `::invalid_filter_value` (INT-002/003) | ✓ |
| 4 Documented filters/values | Tool description enumerates allowed values | ✓ |
| 5 Integration deterministic | 4 search integration tests | ✓ |

### Gate Status

Gate: PASS → docs/qa/gates/2.3-ticket-search-filter-tool.yml
Risk profile: docs/qa/assessments/2.3-risk-20260723.md
Test design: docs/qa/assessments/2.3-test-design-20260723.md

### Recommended Status

✓ Ready for Done
