# Story 1.2: Network Transport Mode (Instructor Hosting)

## Status

Done

## Story

**As an** instructor hosting the server for the class,
**I want** to run the same server over a network transport,
**so that** many students can connect to a single instructor-hosted endpoint.

## Acceptance Criteria

1. The server supports a **network transport** (streamable HTTP/SSE) exposing the identical tool surface as stdio mode, from the same codebase.
2. The transport mode is selectable at startup (CLI flag and/or config), with host and port configurable.
3. A remote MCP client can connect over the network transport, invoke the health/identity tool, and receive the correct response.
4. The server accepts multiple concurrent client connections and responds correctly to each (validated by a test simulating concurrent clients).
5. An integration test exercises the health tool over the network transport.
6. No external services, credentials, or internet access are required to run the network mode.

## Tasks / Subtasks

- [x] Task 1: Add network transport selection to CLI/config (AC: 1, 2)
  - [x] Extend `--transport` choices to include `http` (streamable HTTP); add `--host` and `--port` args with defaults (127.0.0.1:8000)
  - [x] Thread host/port into the FastMCP server settings
- [x] Task 2: Run streamable HTTP from the same server build (AC: 1, 6)
  - [x] `__main__.py` dispatches `http` → `mcp.run(transport="streamable-http")`; identical tool surface (no separate registration path)
  - [x] Confirm no external/network dependencies beyond the local bind
- [x] Task 3: Integration test over the network transport (AC: 3, 5)
  - [x] Start the server (uvicorn) on an ephemeral localhost port in a background thread
  - [x] Connect via the MCP streamable-HTTP client; call `get_server_info`; assert identity
- [x] Task 4: Concurrency test (AC: 4)
  - [x] Open multiple concurrent client sessions against the running server; assert each gets a correct response
- [x] Task 5: README network-mode docs (AC: 2)
  - [x] Document the instructor-hosted command with host/port

## Dev Notes

### Technical Constraints
- **Transport:** streamable HTTP/SSE via FastMCP `mcp.run(transport="streamable-http")`, same codebase as stdio. [Source: architecture/tech-stack.md] [Source: architecture/components.md#Transport-Layer]
- **ASGI server:** Uvicorn hosts the HTTP app. [Source: architecture/tech-stack.md]
- **Config:** transport/host/port selectable at startup. [Source: architecture/components.md#Config-CLI]
- **Concurrency:** read-only, stateless responses → naturally concurrency-safe for classroom scale. [Source: architecture/high-level-architecture.md] [Source: prd/requirements.md] (NFR4)
- **Offline:** binds a local port only; no external services/credentials. [Source: prd/requirements.md] (NFR1)
- **Auth:** none in MVP; open endpoint on trusted LAN (confirmed). [Source: architecture/security.md#Authentication-Authorization]

### File Locations
- CLI/config: `src/contoso_support_mcp/__main__.py`, `config.py`
- Server (unchanged surface): `src/contoso_support_mcp/server.py`
- Tests: `tests/integration/test_server_http.py`
[Source: architecture/source-tree.md]

### Previous Story Insights (1.1)
- FastMCP built via `build_server()`; health tool `get_server_info` returns structured `ServerInfo`. Reuse the same build for HTTP — do not fork the surface.
- Keep all logging on stderr (Critical Rule). [Source: architecture/coding-standards.md#Critical-Rules]
- mcp SDK is 1.28.1; verify streamable-HTTP client API by introspection before writing the test (carried-forward gate note).

### Testing
- **Frameworks:** pytest, pytest-asyncio. [Source: architecture/test-strategy-and-standards.md]
- **This story:** integration test over a real localhost HTTP bind (background uvicorn) + a concurrency test with multiple simultaneous sessions. [Source: architecture/test-strategy-and-standards.md#Integration-Tests]
- **Run:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-23 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-23 | 1.2 | Implemented; 9/9 tests pass, ruff clean; status → Review | James (Dev) |
| 2026-07-23 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-23 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- Introspected SDK: `FastMCP.streamable_http_app()`, client `streamable_http_client(url)` + `ClientSession`; default endpoint path `/mcp`.
- `uv run pytest -q` — **9 passed** (2 unit CLI, 3 HTTP integration, + 4 from 1.1).
- `uv run ruff check` — clean.
- Switched client import from deprecated `streamablehttp_client` → `streamable_http_client` (no warnings).

### Completion Notes List

- Extended CLI with `http` transport + `--host`/`--port`; `build_server(host, port)` threads them into FastMCP settings. stdio path unchanged; **identical tool surface** for both transports (single `build_server`).
- Added `uvicorn>=0.30` as an explicit runtime dependency (network transport host).
- Integration tests run a real localhost bind via background uvicorn on an ephemeral port (startup-race guarded), then connect with the streamable-HTTP client.
- Concurrency test opens 5 simultaneous client sessions; all return `status: ok`.
- Default bind is `127.0.0.1` (loopback); `0.0.0.0` is an explicit instructor opt-in, documented in README.

### File List

- `pyproject.toml` (modified — add uvicorn dep)
- `uv.lock` (regenerated)
- `README.md` (modified — network-mode section)
- `src/contoso_support_mcp/server.py` (modified — host/port params)
- `src/contoso_support_mcp/__main__.py` (modified — http transport + host/port args)
- `tests/unit/test_cli.py` (new)
- `tests/integration/conftest.py` (new — background HTTP server fixture)
- `tests/integration/test_server_http.py` (new)

## QA Results

### Review Date: 2026-07-23

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Clean extension of Story 1.1. Both transports share a single `build_server()` — no forked tool surface — which is exactly the "same codebase" guarantee AC1 demands. The background-uvicorn test fixture is well-built (ephemeral port, startup-race guard, clean shutdown). Dev proactively replaced the deprecated client symbol, leaving zero warnings.

### Refactoring Performed

None required — implementation was already clean.

### Compliance Check

- Coding Standards: ✓ (stderr logging; no stdout in `src/`; typed CLI)
- Project Structure: ✓ (matches source-tree.md)
- Testing Strategy: ✓ (real-bind integration + concurrency, per test-strategy)
- All ACs Met: ✓ (1–6)

### Requirements Traceability

| AC | Validated by | Result |
|----|--------------|--------|
| 1 Network transport, identical surface | `test_server_http.py::surface_matches` (1.2-INT-003) | ✓ |
| 2 Transport/host/port selectable | `test_cli.py` (1.2-UNIT-001) | ✓ |
| 3 Remote client invokes health | `test_server_http.py::health_over_http` (1.2-INT-001) | ✓ |
| 4 Concurrent connections | `test_server_http.py::concurrent_clients` (1.2-INT-002) | ✓ |
| 5 Integration test over network | 1.2-INT-001/002/003 | ✓ |
| 6 No external deps | Local bind only; offline (verified) | ✓ |

### Security Review

Open endpoint / no auth is an **explicitly accepted MVP decision** (trusted classroom LAN, user-confirmed). Default bind is loopback; `0.0.0.0` is an explicit, documented instructor opt-in. Acceptable for scope.

### Performance Considerations

Concurrency validated (5 simultaneous sessions). Stateless read-only handlers scale for classroom use.

### Gate Status

Gate: PASS → docs/qa/gates/1.2-network-transport-mode.yml
Risk profile: docs/qa/assessments/1.2-risk-20260723.md
Test design: docs/qa/assessments/1.2-test-design-20260723.md

### Recommended Status

✓ Ready for Done
