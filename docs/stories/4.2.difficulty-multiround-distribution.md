# Story 4.2: Difficulty Tagging & Multi-Round Distribution

## Status

Done

## Story

**As an** instructor,
**I want** scenarios tagged by difficulty with a verified multi-round proportion,
**so that** I can select scenarios of appropriate challenge and guarantee the intended learning progression.

## Acceptance Criteria

1. Each scenario carries a difficulty indicator (e.g., single-round vs. multi-round, and/or a difficulty level).
2. **At least 25%** of scenarios are multi-round, verified by an automated check over the library.
3. Multi-round scenarios each retain a documented intended investigation path (from Epic 3) to support grading.
4. A distribution report (counts by difficulty/round-type and by domain) is produced and documented.
5. The difficulty tags are queryable/visible to instructors (e.g., via a documented field or a filter), enabling scenario selection for a given lab.

## Tasks / Subtasks

- [x] Task 1: Raise multi-round proportion to ≥25% (AC: 2)
  - [x] Select ~27% of generated scenarios as multi_round (keep TICKET-10000026 multi-round for the e2e test); regenerate
- [x] Task 2: Distribution report (AC: 4)
  - [x] Extend `scripts/coverage_report.py` with a difficulty × category cross-tab; regenerate `docs/scenario-coverage.md`
- [x] Task 3: Automated ≥25% check + path check (AC: 2, 3)
  - [x] Test asserting multi_round ≥ 25% of the library; every multi_round has a ≥2-step path (already in `check_dataset`)
- [x] Task 4: Difficulty documented/visible (AC: 1, 5)
  - [x] `difficulty` is a documented model field; coverage report exposes the distribution for instructor selection

## Dev Notes

### Multi-Round Selection [Source: Story 3.5]
- Change the generator's multi flag to `num % 3 == 2` over the generated range → ~27 multi_round (~26%). This keeps TICKET-10000026 multi-round (used by the 3.5 e2e test).
- Multi-round machinery (benign obvious table + evidence elsewhere + 2-step path) is unchanged.

### Distribution [Source: prd/epic-4-full-scenario-library-guided-prompts.md]
- Coverage report gains a difficulty × root-cause-category cross-tab. `difficulty` is the documented field instructors filter on.

### File Locations [Source: architecture/source-tree.md]
- `scripts/author_seed_scenarios.py` (change multi selection)
- `scripts/coverage_report.py` (add cross-tab)
- `docs/scenario-coverage.md` (regenerated)
- Tests: extend `tests/consistency/`

### Testing
- pytest. Assert multi_round / total ≥ 0.25; consistency green; TICKET-10000026 still multi-round (e2e unaffected).
- **Run:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-23 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-23 | 1.2 | Implemented; 26% multi-round, 83/83 tests pass; status → Review | James (Dev) |
| 2026-07-23 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-23 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- Multi-round selection → `num % 3 == 2`; regenerated. **27 multi_round of 103 (26%)**. `uv run pytest -q` — **83 passed** (+1). ruff clean.
- Coverage report now includes a difficulty × category cross-tab.

### Completion Notes List

- Raised multi-round proportion to ≥25% (26%) via a deterministic selection rule that keeps TICKET-10000026 multi-round (multi-round e2e unaffected).
- Extended `scripts/coverage_report.py` with a difficulty × root-cause-category cross-tab; regenerated `docs/scenario-coverage.md`.
- Added an automated test asserting multi_round/total ≥ 0.25; multi_round ≥2-step-path check already enforced by `check_dataset`.
- `difficulty` is the documented, instructor-facing field; the coverage report exposes the full distribution for lab selection.

### File List

- `scripts/author_seed_scenarios.py` (modified — `_is_multi_round`)
- `scripts/coverage_report.py` (modified — cross-tab)
- `docs/scenario-coverage.md` (regenerated)
- `src/contoso_support_mcp/fixtures/scenarios/*.yaml` (regenerated difficulty tags)
- `tests/consistency/test_scenario_consistency.py` (modified — ≥25% test)

## QA Results

### Review Date: 2026-07-23 — Reviewed By: Quinn (Test Architect)

**Code Quality:** The PRD's headline metric (≥25% multi-round) is now a CI-enforced test rather than a hope — exactly right. The selection rule is deterministic and deliberately preserves the e2e multi-round fixture. The difficulty × category cross-tab is genuinely useful for an instructor picking scenarios of a given challenge and domain.

**Compliance:** Standards ✓ | Structure ✓ | Testing ✓ | ACs ✓ (1-5)

**Traceability:** AC1→difficulty field; AC2→≥25% test (26%); AC3→≥2-step-path check; AC4→cross-tab in coverage report; AC5→documented field + report. All ✓.

### Gate Status

Gate: PASS → docs/qa/gates/4.2-difficulty-multiround-distribution.yml
Risk profile: docs/qa/assessments/4.2-risk-20260723.md
Test design: docs/qa/assessments/4.2-test-design-20260723.md

### Recommended Status

✓ Ready for Done
