# Story 4.4: Full-Library Consistency Validation & Classroom Readiness

## Status

Done

## Story

**As an** instructor,
**I want** whole-library validation and classroom-ready materials,
**so that** I can trust the data and run graded labs on day one.

## Acceptance Criteria

1. The consistency check runs across the entire 100+ library and verifies, for every scenario, that ticket↔resource↔telemetry↔root-cause are coherent; it passes in CI/test and fails on any inconsistency.
2. An instructor-facing **scenario index** is produced (id, product/domain, persona, difficulty/round-type, one-line summary, and the intended root cause/investigation path) to support mapping labs to scenarios and grading.
3. Classroom run docs are finalized for both transport modes, including how to select scenarios and verify connectivity.
4. A full test run (unit + integration + end-to-end) passes against the complete library and tool/prompt surface.
5. The determinism guarantee is verified across the full library (identical inputs → identical outputs) as an automated check.

## Tasks / Subtasks

- [x] Task 1: CI runs the consistency + full test suite (AC: 1, 4)
  - [x] `.github/workflows/ci.yml` runs ruff + pytest (unit/integration/e2e/consistency) on push/PR
- [x] Task 2: Scenario index (AC: 2)
  - [x] `scripts/scenario_index.py` writes `docs/scenario-index.md` (id, product, persona, difficulty, domain, title, root cause, investigation tool sequence)
- [x] Task 3: Determinism automated check (AC: 5)
  - [x] Test: two full-library loads yield identical `model_dump`; a tool called twice yields identical rows
- [x] Task 4: Classroom docs finalize (AC: 3)
  - [x] README: "Selecting scenarios" note pointing to the scenario index + coverage; connectivity check already documented

## Dev Notes

### Consistency & Determinism [Source: architecture/test-strategy-and-standards.md]
- `check_dataset` already validates ticket↔resource↔telemetry↔root-cause; the consistency suite runs it over the whole library. Add an explicit full-library determinism test.

### Scenario Index [Source: prd/epic-4-full-scenario-library-guided-prompts.md]
- Instructor-facing table for lab mapping + grading. Include the intended investigation tool sequence (from each scenario's `investigation_path`) and the root-cause summary (the "correct answer" for grading).

### CI [Source: architecture/infrastructure-and-deployment.md#Deployment-Strategy]
- GitHub Actions: `uv sync` → `uv run ruff check` → `uv run pytest`.

### File Locations [Source: architecture/source-tree.md]
- `.github/workflows/ci.yml` (new)
- `scripts/scenario_index.py` (new)
- `docs/scenario-index.md` (new, generated)
- `README.md` (scenario-selection note)
- Tests: extend `tests/consistency/`

### Testing
- pytest full suite green; determinism test over the full library.
- **Run:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-23 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-23 | 1.2 | Implemented; CI + index + determinism, 89/89 tests; status → Review | James (Dev) |
| 2026-07-23 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-23 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- `uv run pytest -q` — **89 passed** (+3). ruff clean. Scenario index: 103 rows.

### Completion Notes List

- `.github/workflows/ci.yml` runs `uv sync` → `uv run ruff check` → `uv run pytest` on push/PR (unit + integration + e2e + consistency).
- `scripts/scenario_index.py` writes `docs/scenario-index.md` — one row per scenario with the expected root cause (grading answer) and intended investigation tool sequence.
- Determinism verified across the full library: two loads yield identical `model_dump`; a telemetry tool called twice returns identical rows.
- README gains a "For instructors" section (scenario index + coverage + regeneration commands + trust statement).

### File List

- `.github/workflows/ci.yml` (new)
- `scripts/scenario_index.py` (new)
- `docs/scenario-index.md` (new, generated — 103 rows)
- `README.md` (modified — instructor section)
- `tests/consistency/test_determinism_and_index.py` (new)

## QA Results

### Review Date: 2026-07-23 — Reviewed By: Quinn (Test Architect)

**Code Quality:** A fitting capstone. The whole-library consistency + determinism guarantees are now CI-enforced, and the scenario index turns the dataset into a gradeable teaching asset (expected root cause + intended tool sequence per scenario). Determinism is proven both at the load layer and through the actual tool surface. Nothing is asserted that isn't also runnable in CI.

**Compliance:** Standards ✓ | Structure ✓ | Testing ✓ | ACs ✓ (1-5)

**Traceability:** AC1→consistency suite over 103; AC2→scenario-index.md (103 rows) + index test; AC3→README instructor/run docs; AC4→full 89-test suite green; AC5→load + query determinism tests. All ✓.

**MVP note:** with 4.4 closed, all PRD MVP acceptance criteria are met — connectable dual-transport server, ticket + telemetry tools, 100+ consistent scenarios (≥25% multi-round), guided prompts, and classroom-ready validation/materials.

### Gate Status

Gate: PASS → docs/qa/gates/4.4-full-consistency-classroom-readiness.yml
Risk profile: docs/qa/assessments/4.4-risk-20260723.md
Test design: docs/qa/assessments/4.4-test-design-20260723.md

### Recommended Status

✓ Ready for Done
