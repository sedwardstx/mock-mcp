# Story 4.4: Full-Library Consistency Validation & Classroom Readiness

## Status

Approved

## Story

**As an** instructor,
**I want** whole-library validation and classroom-ready materials,
**so that** I can trust the data and run graded labs on day one.

## Acceptance Criteria

1. The consistency check runs across the entire 100+ library and verifies, for every scenario, that ticket↔resource↔telemetry↔root-cause are coherent; it passes in CI/test and fails on any inconsistency.
2. An instructor-facing **scenario index** is produced (id, product/domain, persona, difficulty/round-type, one-line summary, and the intended root cause/investigation path) to support mapping labs to scenarios and grading.
3. Classroom run docs are finalized for both transport modes, including how to select scenarios and verify connectivity.
4. A full test run (unit + integration + end-to-end) passes against the complete library and tool/prompt surface.
5. The determinism guarantee is verified across the full library (identical inputs → identical outputs) as an automated check.

## Tasks / Subtasks

- [ ] Task 1: CI runs the consistency + full test suite (AC: 1, 4)
  - [ ] `.github/workflows/ci.yml` runs ruff + pytest (unit/integration/e2e/consistency) on push/PR
- [ ] Task 2: Scenario index (AC: 2)
  - [ ] `scripts/scenario_index.py` writes `docs/scenario-index.md` (id, product, persona, difficulty, domain, title, root cause, investigation tool sequence)
- [ ] Task 3: Determinism automated check (AC: 5)
  - [ ] Test: two full-library loads yield identical `model_dump`; a tool called twice yields identical rows
- [ ] Task 4: Classroom docs finalize (AC: 3)
  - [ ] README: "Selecting scenarios" note pointing to the scenario index + coverage; connectivity check already documented

## Dev Notes

### Consistency & Determinism [Source: architecture/test-strategy-and-standards.md]
- `check_dataset` already validates ticket↔resource↔telemetry↔root-cause; the consistency suite runs it over the whole library. Add an explicit full-library determinism test.

### Scenario Index [Source: prd/epic-4-full-scenario-library-guided-prompts.md]
- Instructor-facing table for lab mapping + grading. Include the intended investigation tool sequence (from each scenario's `investigation_path`) and the root-cause summary (the "correct answer" for grading).

### CI [Source: architecture/infrastructure-and-deployment.md#Deployment-Strategy]
- GitHub Actions: `uv sync` → `uv run ruff check` → `uv run pytest`.

### File Locations [Source: architecture/source-tree.md]
- `.github/workflows/ci.yml` (new)
- `scripts/scenario_index.py` (new)
- `docs/scenario-index.md` (new, generated)
- `README.md` (scenario-selection note)
- Tests: extend `tests/consistency/`

### Testing
- pytest full suite green; determinism test over the full library.
- **Run:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-23 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

_Not yet implemented._

### Debug Log References

_None yet._

### Completion Notes List

_None yet._

### File List

_None yet._

## QA Results

_Pending QA review._
