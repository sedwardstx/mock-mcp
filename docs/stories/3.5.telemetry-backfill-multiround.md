# Story 3.5: Telemetry Backfill & Multi-Round Scenario Authoring

## Status

Done

## Story

**As an** instructor,
**I want** existing scenarios backfilled with correlated telemetry and new multi-round scenarios added,
**so that** agents can perform full RCA and some incidents require iterative investigation.

## Acceptance Criteria

1. All seed scenarios from Epic 2 are backfilled with correlated telemetry across the relevant tables, so each has an evidence trail supporting its defined root cause.
2. Additional scenarios are authored to reach a cumulative total of ~60 distinct scenarios, spanning ARM/Network/Compute root causes across both personas.
3. At least a handful of scenarios are **multi-round**: the ticket symptom alone is insufficient, and the supporting evidence is distributed such that an agent must query telemetry, form a hypothesis, and query again (a different table, time range, or resource/instance) to reach the root cause.
4. For each multi-round scenario, the intended investigation path (which queries reveal which evidence) is documented to support lab authoring and grading.
5. The consistency check is extended to verify, for every scenario, that the telemetry actually contains evidence consistent with the defined root cause; it runs in the test suite and fails on any inconsistency.
6. End-to-end tests demonstrate at least one single-round and one multi-round scenario solved via the tool sequence (ticket → resource → telemetry → root cause).

## Tasks / Subtasks

- [x] Task 1: Telemetry-aware generator (AC: 1, 2)
  - [x] Extend `scripts/author_seed_scenarios.py` to emit category-correct telemetry (evidence row + benign noise) for every generated scenario; grow the curated/generated set to ~60 total
- [x] Task 2: Multi-round scenarios (AC: 3, 4)
  - [x] A handful of `multi_round` scenarios where the "obvious" table is inconclusive and the real evidence lives in another table/instance; 2-step `investigation_path` documented
- [x] Task 3: Consistency: telemetry evidence (AC: 5)
  - [x] Extend `check_dataset` so each scenario's root_cause.category has matching evidence in the correct table for its resource; negative test
- [x] Task 4: End-to-end tests (AC: 6)
  - [x] `tests/e2e/`: single-round RCA (ticket→resource→telemetry→cause) and a multi-round RCA (first query inconclusive, second reveals cause)

## Dev Notes

### Evidence Heuristics (per category) [Source: architecture/database-schema.md]
- `arm`: an ArmTraceRow with `activity_status == "Failed"` (or http_status_code ≥ 400).
- `network`: a NetworkLogRow with `action == "Deny"`.
- `compute_host`: a ComputeHostLogRow with `health_status in {"Degraded","Unavailable"}` or `level == "Error"`.
- `compute_guest`: a ComputeGuestLogRow with `level in {"Error","Warning"}` (Windows-only).

### Multi-Round Design [Source: prd/epic-3-kusto-telemetry-tools-correlation.md]
- The "obvious" first query (a table implied by the symptom) returns only benign rows; the true evidence is in a different table/instance. `investigation_path` has 2 steps: step 1 (obvious, inconclusive), step 2 (reveals the cause). `difficulty: multi_round`.

### Generator [Source: Story 2.5]
- The generator now owns telemetry for scenarios 4..60 (single source of truth). Hand-authored 1–3 keep their telemetry. Re-running the generator is deterministic (no clock/random).

### Consistency Extension [Source: architecture/test-strategy-and-standards.md#Consistency-Tests]
- `check_dataset` gains a per-scenario evidence check (category → table → evidence row for the resource). Keeps prior ticket↔resource checks.

### File Locations [Source: architecture/source-tree.md]
- `scripts/author_seed_scenarios.py` (rewrite — telemetry + multi-round + reach ~60)
- `src/contoso_support_mcp/fixtures/scenarios/*.yaml` (regenerated 4..60)
- `src/contoso_support_mcp/data/consistency.py` (extend)
- Tests: `tests/e2e/` (new), extend consistency tests

### Testing
- pytest. Consistency suite must be green over all ~60. E2E drives the actual MCP tools for one single-round and one multi-round scenario.
- **Run:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-23 | 1.1 | Validated — GO (8/10); status → Approved | Sarah (PO) |
| 2026-07-23 | 1.2 | Implemented; 60 scenarios, 80/80 tests pass; status → Review | James (Dev) |
| 2026-07-23 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-23 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- Regenerated 57 scenarios (4-60) with telemetry; **60 total**, 7 multi_round, categories balanced (arm 15 / network 16 / guest 15 / host 14).
- `uv run pytest -q` — **80 passed** (+5: 3 consistency/unit, 2 e2e). ruff clean.
- Consistency suite green over all 60 (telemetry-evidence check).

### Completion Notes List

- Rewrote the generator to emit category-correct telemetry (evidence + benign noise) for every generated scenario, reaching 60 total. Hand-authored 1-3 keep their telemetry; the generator owns 4-60 deterministically. TICKET-10000008 regenerated to match the 3.4 VMSS-host fixture (data-vmss_0 Degraded / data-vmss_1 Healthy).
- 7 **multi_round** scenarios: the "obvious" table holds only benign rows; the real evidence is in another table, with a documented 2-step `investigation_path`.
- Extended `check_dataset` with a per-scenario telemetry-evidence check (category → table → evidence row) and a multi_round ≥2-step-path check; negative tests prove both fire.
- Added `tests/e2e/` — single-round ARM RCA and a multi-round RCA (obvious query inconclusive, second reveals the degraded-host evidence).
- **Note:** two earlier tests updated (they assumed generated scenarios had empty telemetry / a 20-30 size cap) — generalized for the grown, telemetry-bearing library.

### File List

- `scripts/author_seed_scenarios.py` (rewritten — telemetry + multi-round + 60)
- `src/contoso_support_mcp/fixtures/scenarios/TICKET-10000004.yaml`..`TICKET-10000060.yaml` (regenerated/added)
- `src/contoso_support_mcp/data/consistency.py` (extended — evidence + multi-round checks)
- `tests/consistency/test_scenario_consistency.py` (extended)
- `tests/unit/test_telemetry_models.py` (updated backward-compat test)
- `tests/e2e/__init__.py`, `tests/e2e/test_rca_flows.py` (new)

## QA Results

### Review Date: 2026-07-23 — Reviewed By: Quinn (Test Architect)

**Code Quality:** This is the story that makes the whole product teachable, and it's done well. The telemetry-evidence consistency check is the key control — it makes it *impossible* for a scenario to ship claiming a root cause its logs don't support, and it will guard Epic 4's 100+ expansion automatically. Multi-round design is honest: the e2e test proves the obvious query returns no Error-level rows while the second query surfaces the Degraded evidence — i.e., iteration is genuinely required, not decorative. Generator remains deterministic.

**Compliance:** Standards ✓ | Structure ✓ | Testing ✓ (e2e tier added) | ACs ✓ (1-6)

**Traceability:** AC1/5→CON-001 + evidence negative test; AC2→size test (60); AC3/4→multi-round test + CON-002; AC6→2 e2e flows. All ✓.

**Coverage note:** 7/60 multi_round (~12%). PRD's ≥25% target is an Epic 4.2 requirement; 3.5's "a handful" is satisfied. Flagged for 4.2.

### Gate Status

Gate: PASS → docs/qa/gates/3.5-telemetry-backfill-multiround.yml
Risk profile: docs/qa/assessments/3.5-risk-20260723.md
Test design: docs/qa/assessments/3.5-test-design-20260723.md

### Recommended Status

✓ Ready for Done
