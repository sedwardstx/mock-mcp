# Story 3.1: Telemetry Data Model & Mock Kusto Table Schemas

## Status

Done

## Story

**As a** server developer,
**I want** mock Kusto table schemas and a telemetry data model linked to scenarios and resources,
**so that** every telemetry tool queries one consistent, believable source correlated to tickets.

## Acceptance Criteria

1. Documented mock table schemas approximate real Azure log schemas for: **ARM control-plane traces**, **Network logs**, **Compute host/platform logs** (VM and VMSS), and **Compute Windows guest logs** (VM and VMSS), each with realistic, log-type-appropriate columns (including timestamps and a resource identifier).
2. The scenario data model is extended so telemetry rows are associated with a scenario and its resource(s), and reference the scenario's root cause (i.e., logs contain the evidence supporting that root cause).
3. The fixture loader loads telemetry deterministically alongside tickets, with schema validation and fail-fast on malformed telemetry.
4. A common query semantic is defined for all telemetry tools — scoping by resource identifier and time range, plus log-type-appropriate filters — returning structured rows.
5. Sample telemetry for a few existing scenarios is included and loads successfully, sufficient to develop and test the query tools.
6. Unit tests cover telemetry loading, schema validation (including a negative case), and determinism.

## Tasks / Subtasks

- [x] Task 1: Typed telemetry row models (AC: 1, 2)
  - [x] `ArmTraceRow`, `NetworkLogRow`, `ComputeHostLogRow`, `ComputeGuestLogRow` with the columns from database-schema.md; replace `Telemetry` `list[dict]` with typed lists
- [x] Task 2: Common query semantic (AC: 4)
  - [x] `data/query.py::query_rows(rows, resource_id, time_range, instance_id, filters) -> list[row]` — deterministic filter by resource, optional time range, optional instance, and field filters
- [x] Task 3: Loader validates telemetry (AC: 3)
  - [x] Telemetry rows validated via the typed models on load; malformed telemetry fails fast naming the file
- [x] Task 4: Sample telemetry (AC: 5)
  - [x] Backfill 2 existing scenarios (TICKET-10000001 ARM, TICKET-10000002 Network) with evidence rows + realistic noise
- [x] Task 5: Unit tests (AC: 6)
  - [x] Row model validation (valid + negative); loader loads typed telemetry; `query_rows` scoping/time-range/filter + determinism

## Dev Notes

### Mock Kusto Schemas [Source: architecture/database-schema.md]
- **ArmControlPlaneTraces:** time_generated, resource_id, subscription_id, resource_group, correlation_id, operation_name, caller, client_ip, http_status_code(int), level, activity_status, sub_status, properties.
- **NetworkLogs:** time_generated, resource_id, subscription_id, flow_direction, source_ip, destination_ip, source_port(int), destination_port(int), protocol, action, nsg_rule_name, bytes_sent(int), bytes_received(int), packets_sent(int), packets_received(int).
- **ComputeHostLogs:** time_generated, resource_id, subscription_id, instance_id, host_node, event_name, health_status, maintenance_type, level, message.
- **ComputeGuestLogs:** time_generated, resource_id, subscription_id, instance_id, computer, channel, provider_name, event_id(int), level, task, message.
- Each row carries `time_generated` (drives time scoping) and `resource_id` (drives resource scoping). `instance_id` empty for a plain VM.

### Query Semantic [Source: architecture/data-models.md#TelemetryRecord] [Source: architecture/components.md#Repository]
- All telemetry tools scope by `resource_id` + optional `time_range` ("startISO/endISO") + log-type filters; `instance_id` for VMSS. Deterministic; no clock/random.

### Constraints
- Typed telemetry keeps `extra="forbid"`. Existing fixtures use `telemetry: {}` → still valid (all lists default empty). [Source: architecture/coding-standards.md#Critical-Rules]
- Determinism preserved. [Source: architecture/coding-standards.md#Critical-Rules]

### File Locations [Source: architecture/source-tree.md]
- `src/contoso_support_mcp/data/models.py` (extend — row models, typed Telemetry)
- `src/contoso_support_mcp/data/query.py` (new)
- fixtures TICKET-10000001/2 (add telemetry)
- Tests: `tests/unit/test_telemetry_models.py`, `tests/unit/test_query.py`, extend `test_loader.py`

### Previous Story Insights (2.1, 2.5)
- `Telemetry` container already exists (empty). Consistency validator (`check_dataset`) will be extended in 3.5 to check telemetry-vs-root-cause; not required here.

### Testing
- pytest. Negative case: a telemetry row missing `time_generated`/`resource_id` → ValidationError. Determinism: `query_rows` returns stable order.
- **Run:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-23 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-23 | 1.2 | Implemented; 61/61 tests pass, ruff clean; status → Review | James (Dev) |
| 2026-07-23 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-23 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- `uv run pytest -q` — **61 passed** (+8: 4 telemetry-model, 4 query).
- `uv run ruff check` — clean (2 E501 wraps in query.py).

### Completion Notes List

- Four typed row models (`ArmTraceRow`, `NetworkLogRow`, `ComputeHostLogRow`, `ComputeGuestLogRow`) matching database-schema.md columns; `Telemetry` now holds typed lists. `telemetry: {}` still valid (all default empty) — backward-compat verified.
- `data/query.py`: `query_rows(...)` common semantic — scopes by resource_id, optional time_range ("START/END" ISO, inclusive, blank sides allowed), optional VMSS instance_id, and field filters; preserves input order (deterministic). `parse_time_range` + `TimeRangeError` for clear failures. `as_dicts` serializes rows for tool responses.
- Backfilled sample telemetry: TICKET-10000001 (ARM traces incl. 409 AllocationFailed + benign noise), TICKET-10000002 (network Allow + the Deny evidence).

### File List

- `src/contoso_support_mcp/data/models.py` (modified — row models, typed Telemetry)
- `src/contoso_support_mcp/data/query.py` (new)
- `src/contoso_support_mcp/fixtures/scenarios/TICKET-10000001.yaml` (modified — ARM telemetry)
- `src/contoso_support_mcp/fixtures/scenarios/TICKET-10000002.yaml` (modified — network telemetry)
- `tests/unit/test_telemetry_models.py` (new)
- `tests/unit/test_query.py` (new)

## QA Results

### Review Date: 2026-07-23 — Reviewed By: Quinn (Test Architect)

**Code Quality:** Excellent foundation for the telemetry tools. Typed rows make malformed telemetry fail at load. Centralizing scoping in one `query_rows` (used by all four upcoming tools) directly mitigates the "inconsistent semantic" risk — one place to get time-range and instance scoping right. Time-range parsing handles Z-suffix and open-ended sides, with inclusive bounds tested at the boundary.

**Compliance:** Coding Standards ✓ (determinism) | Project Structure ✓ | Testing Strategy ✓ | All ACs ✓

**Traceability:** AC1→row model tests; AC2→typed telemetry load; AC3→negative model test + loader; AC4→query tests (scope/time/filter); AC5→backfilled samples load; AC6→8 unit tests. All ✓.

**Security/Perf/Reliability/Maintainability:** all PASS — mocked data, in-memory, deterministic, single shared semantic.

### Gate Status

Gate: PASS → docs/qa/gates/3.1-telemetry-data-model-kusto-schemas.yml
Risk profile: docs/qa/assessments/3.1-risk-20260723.md
Test design: docs/qa/assessments/3.1-test-design-20260723.md

### Recommended Status

✓ Ready for Done
