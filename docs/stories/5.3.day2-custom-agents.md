# Story 5.3: Day-2 Custom Agents

## Status

Done

## Story

**As an** instructor,
**I want** the specialist and coordinator custom agents wired to this server's tools,
**so that** participants can run the Day-2 agent + coordinator labs.

## Acceptance Criteria

1. Three specialist agents exist — compute-diagnostics, network-diagnostics, controlplane-diagnostics — each referencing this server and naming the specific telemetry tools it uses.
2. A `support-triage` coordinator agent (tools: ['agent']) routes to the three specialists and offers a "Draft the resolution" handoff.
3. A minimal `ticket-writer` agent exists so the coordinator handoff resolves out of the box.
4. Agent frontmatter follows the workbook's conventions; specialist names exactly match the coordinator's `agents:` list.

## Tasks / Subtasks

- [x] Task 1: Specialists (AC: 1)
  - [x] `.github/agents/compute-diagnostics.agent.md` (query_compute_host_logs/query_compute_guest_logs; VMSS instance_id; host↔guest iteration)
  - [x] `.github/agents/network-diagnostics.agent.md` (query_network_logs; action='Deny'/nsg_rule_name)
  - [x] `.github/agents/controlplane-diagnostics.agent.md` (query_arm_traces; activity_status='Failed'/sub_status; search_known_issues)
- [x] Task 2: Coordinator + ticket-writer (AC: 2, 3)
  - [x] `.github/agents/support-triage.agent.md` (tools:['agent']; agents:[the three]; handoffs → ticket-writer)
  - [x] `.github/agents/ticket-writer.agent.md` (drafts RCA behind human-review gate)
- [x] Task 3: Verify (AC: 4)
  - [x] Each agent has YAML frontmatter with required fields; coordinator's `agents:` list matches the 3 specialist filenames; regression `uv run pytest`/`ruff` still green

## Dev Notes

### Agent conventions [Source: docs/external/Participant_Workbook.md] (Appendix A; Lab 5/6)
- Frontmatter fields: `description`, `name`, `tools`, optional `model`, `agents`, `handoffs`, `user-invocable`.
- **`tools:` references the MCP server by its `mcp.json` key** `contoso-support` (per architecture D1) — the persona body names the specific tools. [Source: architecture/workshop-integration-epic-5.md]
- Specialists also use `codebase`. Coordinator uses `tools: ['agent']` + `agents: [...]`.

### Domain map [Source: prd/epic-5-workshop-integration.md]
- compute-diagnostics → `query_compute_host_logs`, `query_compute_guest_logs` (VM/VMSS host+guest; cite TICKET-10000026 multi-round).
- network-diagnostics → `query_network_logs` (NSG/connectivity; port 1433 example).
- controlplane-diagnostics → `query_arm_traces` (+ `search_known_issues`).
- support-triage → classify Compute/Network/Control-plane, delegate, synthesize; handoff to ticket-writer.

### Server tools [Source: architecture/components.md#MCP-Surface]
- The 8 diagnostic tools + `search_known_issues`; all surfaced under the `contoso-support` server key.

### File Locations [Source: architecture/workshop-integration-epic-5.md]
- `.github/agents/{compute,network,controlplane}-diagnostics.agent.md`, `support-triage.agent.md`, `ticket-writer.agent.md`.

### Testing
- Docs/config — verify each file has valid frontmatter (parse YAML front block) and the coordinator's `agents:` entries exactly match specialist `name:`/filenames. Keep `uv run pytest`/`ruff` green (no server code changed).
- **Run:** frontmatter parse check; `uv run pytest -q`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-24 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-24 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-24 | 1.2 | Implemented; 5 agents verified, 100/100 regression; status → Review | James (Dev) |
| 2026-07-24 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-24 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- Frontmatter/wiring check: 5 agent files, valid YAML frontmatter; coordinator `tools:['agent']`, `agents:` == the 3 specialists, handoff → ticket-writer; each specialist body names its query_* tool(s). Regression: 100 passed, ruff clean.

### Completion Notes List

- Three specialists (compute/network/controlplane-diagnostics) reference the server by its `contoso-support` mcp.json key and name their specific telemetry tools in the body; each stays in-lane and defers cross-domain issues by naming the right specialist.
- `support-triage` coordinator: `tools:['agent']`, `agents:[the three]`, `handoffs` → ticket-writer; classifies Compute/Network/Control-plane, delegates, synthesizes.
- `ticket-writer` drafts an RCA behind a human-review gate (mirrors summarize_rca).
- Verified coordinator `agents:` names exactly match specialist filenames/names (Appendix B delegation gotcha).

### File List

- `.github/agents/compute-diagnostics.agent.md` (new)
- `.github/agents/network-diagnostics.agent.md` (new)
- `.github/agents/controlplane-diagnostics.agent.md` (new)
- `.github/agents/support-triage.agent.md` (new)
- `.github/agents/ticket-writer.agent.md` (new)

## QA Results

### Review Date: 2026-07-24 — Reviewed By: Quinn (Test Architect)

**Code Quality:** The delegation wiring — the thing that most often silently breaks a coordinator — is machine-verified: coordinator `agents:` set exactly equals the three specialist `name:` values, `tools:['agent']` is present, and the handoff target `ticket-writer` exists. Each specialist names its actual server tools and stays in-lane with explicit deferral to the right sibling, which is exactly what Lab 6 teaches. Frontmatter parses for all five.

**Compliance:** Standards ✓ | Structure ✓ | Testing ✓ (verification-level) | ACs ✓ (1–4).

**Traceability:** AC1→3 specialists + tool-naming check; AC2→coordinator shape check; AC3→ticket-writer exists + handoff resolves; AC4→frontmatter + name-match check. All ✓.

### Gate Status

Gate: PASS → docs/qa/gates/5.3-day2-custom-agents.yml
Risk profile: docs/qa/assessments/5.3-risk-20260724.md
Test design: docs/qa/assessments/5.3-test-design-20260724.md

### Recommended Status

✓ Ready for Done
