# Story 3.3: Network Log Query Tool

## Status

Done

## Story

**As a** student's AI agent,
**I want** to query Network logs for a resource,
**so that** I can investigate connectivity, NSG/routing, and network-related incidents.

## Acceptance Criteria

1. A tool (e.g., `query_network_logs`) accepts a resource identifier, an optional time range, and network-appropriate filters, and returns matching log rows.
2. Results are scoped and deterministic for identical inputs.
3. Results are correlated to the scenario — network-root-caused scenarios contain supporting evidence with realistic noise.
4. Empty results return an empty set; malformed inputs return a clear validation message.
5. The tool exposes a clear MCP schema documenting parameters, filters, and returned columns.
6. Integration tests assert correct, deterministic results for representative scenarios.

## Tasks / Subtasks

- [x] Task 1: `query_network_logs` tool (AC: 1, 4, 5)
  - [x] Params: resource_id (req), time_range (opt), action (opt filter, e.g. 'Deny'); reuse `_query` helper over `network_logs` table
- [x] Task 2: Tests (AC: 2, 3, 6)
  - [x] Integration: query VMSS resource (TICKET-10000002) → the Deny evidence row (dst 1433) present; action='Deny' filter; unrelated resource empty; bad time_range invalid

## Dev Notes

### Design [Source: architecture/database-schema.md#Table-NetworkLogs] [Source: architecture/components.md#MCP-Surface]
- Thin wrapper over `Repository.query_telemetry("network_logs", ...)` + the shared `_query` helper from Story 3.2. No new scoping logic.
- Network filter: `action` ('Allow'/'Deny'). Columns include flow_direction, source/destination ip+port, protocol, action, nsg_rule_name, byte/packet counts.

### Evidence [Source: fixtures TICKET-10000002]
- TICKET-10000002 (network root cause) VMSS `api-vmss` has an Allow row (443) and the Deny evidence row (dst port 1433, nsg_rule_name 'DenyDbOutbound').

### File Locations [Source: architecture/source-tree.md]
- `src/contoso_support_mcp/tools/telemetry.py` (extend — `query_network_logs`)
- `tests/integration/test_telemetry_tools.py` (extend)

### Testing
- pytest + in-memory MCP client. Assert the Deny row surfaces for the VMSS resource; action filter narrows; unrelated resource empty.
- **Run:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-23 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-23 | 1.2 | Implemented; 69/69 tests pass, ruff clean; status → Review | James (Dev) |
| 2026-07-23 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-23 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- `uv run pytest -q` — **69 passed** (+4 network integration). ruff clean.

### Completion Notes List

- Added `query_network_logs` tool (resource_id, time_range, action filter) via the shared `_query` helper over the `network_logs` table.
- Verified: VMSS query surfaces the Deny evidence (dst 1433, DenyDbOutbound) alongside the benign Allow (443); action='Deny' narrows to 1; unrelated resource empty ok; bad time_range invalid_request.

### File List

- `src/contoso_support_mcp/tools/telemetry.py` (modified — `query_network_logs`)
- `tests/integration/test_telemetry_tools.py` (modified — 4 network tests + VMSS_RID)

## QA Results

### Review Date: 2026-07-23 — Reviewed By: Quinn (Test Architect)

**Code Quality:** Trivial, correct addition — exactly the payoff of the 3.2 `_query` helper. Evidence correlation is real (asserts the specific Deny row + NSG rule name). Filter and scoping verified.

**Compliance:** Standards ✓ | Structure ✓ | Testing ✓ | ACs ✓ (1–6)

**Traceability:** AC1/3→INT-001; AC1→INT-002 (filter); AC4→INT-003/004; AC5→schema; AC6→4 tests. All ✓.

### Gate Status

Gate: PASS → docs/qa/gates/3.3-network-log-query-tool.yml
Risk profile: docs/qa/assessments/3.3-risk-20260723.md
Test design: docs/qa/assessments/3.3-test-design-20260723.md

### Recommended Status

✓ Ready for Done
