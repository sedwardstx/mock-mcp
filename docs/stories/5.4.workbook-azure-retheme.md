# Story 5.4: Workbook Azure Re-Theme

## Status

Done

## Story

**As an** instructor,
**I want** the participant workbook re-themed to Azure with its MCP labs pointed at this server,
**so that** the whole workshop is consistent and runnable against our real server.

## Acceptance Criteria

1. The workbook's product fiction (Gateway/Vault/Store) and `CONTOSO-####` ids are replaced with our Azure domains (Compute/Networking/ARM control-plane) and real `TICKET-XXXXXXXX` ids.
2. Lab 4's MCP config is replaced with our `.vscode/mcp.json` (stdio + http), the example flow uses real tools (`get_ticket` → `get_ticket_resources` → `query_arm_traces`), and it notes the server's MCP prompts.
3. Labs 5–6 reference the three Azure specialists and the coordinator; the KB-only variant uses `search_known_issues`.
4. Filenames/ids/log codes referenced in the workbook match the Story 5.2 assets exactly; a grep confirms no residual `CONTOSO-`/`Gateway`/`Vault`/`Store`/`@contoso/mcp-ticketing`/`contoso-kb`.

## Tasks / Subtasks

- [x] Task 1: Global fiction re-theme (AC: 1)
  - [x] Subtitle + module intros → Azure domains; all `CONTOSO-####` → real `TICKET-########`
- [x] Task 2: Day-1 lab references (AC: 4)
  - [x] Lab 1 sample file → `samples/logs/arm-allocation-failure.log`; copilot-instructions block → Azure; SQL Stretch → KQL scoped instruction
  - [x] Lab 2 Stretch ticket path → `samples/tickets/TICKET-10000001.md`
  - [x] Lab 3 skill description → Azure; `parse_logs.py` regex → `AZURE-\d{4}`; error-codes table → `AZURE-####`; note KB supersedes on Day 2
- [x] Task 3: Lab 4 MCP (AC: 2)
  - [x] Replace the `inputs`+`contoso-ticketing`+`contoso-kb` block with our stdio + http `.vscode/mcp.json`; example flow uses real tools; add MCP prompts callout; Stretch → toggle transports + least privilege
- [x] Task 4: Labs 5–6 agents (AC: 3)
  - [x] Lab 5 specialist → compute-diagnostics + note (network/controlplane); KB-only Stretch → `search_known_issues`
  - [x] Lab 6 coordinator `agents:` → the three Azure specialists; classify Compute/Network/Control-plane; Azure ambiguous + cross-domain examples (reference TICKET-10000026)
- [x] Task 5: Verify (AC: 4)
  - [x] Grep confirms no residual old-fiction tokens; referenced filenames/ids exist in the repo

## Dev Notes

### Edit map [Source: architecture/workshop-integration-epic-5.md] [Source: plan]
- Keep **Contoso** + the server name; replace only the three products and `CONTOSO-####` ids.
- Domain map: Gateway→Networking (`query_network_logs`), Store→Compute VM/VMSS (`query_compute_host_logs`/`query_compute_guest_logs`), Vault→ARM control-plane (`query_arm_traces`). Vault/Store content is **rewritten** (no auth/db in our server), not relabeled.
- Lab 4 config reuses the `.vscode/mcp.json` blocks from README/Story 5.2.
- Demo ids: `TICKET-10000001` (single-round arm), `TICKET-10000026` (multi-round compute_host).

### Cross-file consistency [Source: story 5.2]
- Filenames must match 5.2 assets: `samples/logs/arm-allocation-failure.log`, `samples/tickets/TICKET-10000001.md`; codes `AZURE-1001/2003/3005/4008`.

### File Locations
- `docs/external/Participant_Workbook.md` (in-place).

### Testing
- Grep the workbook for residual `CONTOSO-`, `Gateway`, `Vault`, `Store`, `gateway-500.log`, `@contoso/mcp-ticketing`, `contoso-kb` → none. Confirm every filename/id the workbook references exists in the repo. No server code changes → `uv run pytest`/`ruff` unaffected.
- **Run:** grep checks; `ls` referenced assets.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-24 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-24 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-24 | 1.2 | Implemented; re-theme complete, grep-clean; status → Review | James (Dev) |
| 2026-07-24 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-24 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- Grep: **zero** residual `CONTOSO-`/`Gateway`/`Vault`/`Store`/`gateway-500.log`/`@contoso/mcp-ticketing`/`contoso-kb`/`db-diagnostics`/`auth-diagnostics` in the workbook.
- All referenced files exist (`arm-allocation-failure.log`, `TICKET-10000001.md`, `.vscode/mcp.json`, the agents); `TICKET-10000026` resolves. No server code changed (suite unaffected).

### Completion Notes List

- Global: subtitle + module intros → Azure domains; all `CONTOSO-####` → `TICKET-########`.
- Day 1: Lab 1 sample → `arm-allocation-failure.log`; copilot-instructions block → Azure; SQL Stretch → **KQL** scoped instruction; Lab 2 Stretch ticket → `TICKET-10000001.md`; Lab 3 skill description → Azure, `parse_logs.py` regex → `AZURE-\d{4}`, error-codes table → `AZURE-####` (+ KB-supersedes note).
- Lab 4: replaced the fictional `inputs`+`contoso-ticketing`+`contoso-kb` block with our **two** `.vscode/mcp.json` blocks (stdio + http, `servers` key); example flow → `get_ticket TICKET-10000001` → `get_ticket_resources` → `query_arm_traces`; added an **MCP prompts** callout; Stretch → toggle transports + least privilege.
- Labs 5–6: specialist → `compute-diagnostics` (+ note on network/controlplane); KB-only Stretch → `search_known_issues`; coordinator `agents:` → the three Azure specialists, classify Compute/Network/Control-plane; ambiguous + cross-domain examples re-themed (references `TICKET-10000026` as the multi-round case).
- Vault/Store (auth/db) content was **rewritten** into compute/control-plane, not relabeled (server has no auth/db domain).

### File List

- `docs/external/Participant_Workbook.md` (modified — in-place Azure re-theme)

## QA Results

### Review Date: 2026-07-24 — Reviewed By: Quinn (Test Architect)

**Code Quality:** The re-theme is complete and internally consistent. The residual-fiction grep is empty (the AC4 acceptance bar), and every filename/id the workbook now cites resolves to a real repo asset or server fixture — so a participant following the book won't hit a dead reference. Lab 4 correctly targets our server with both transports and adds the MCP-prompts teaching point; Labs 5–6 use the exact agent names shipped in Story 5.3. The auth/db content was genuinely rewritten to compute/control-plane rather than relabeled, which is the honest handling given the server has no such domains.

**Compliance:** Standards ✓ | Structure ✓ | ACs ✓ (1–4) | Cross-file consistency ✓ (codes/ids/filenames align with 5.2/5.3).

**Traceability:** AC1→subtitle/instructions/id sweep; AC2→Lab 4 blocks + real-tool flow + prompts note; AC3→Lab 5/6 agents + KB variant; AC4→empty grep + file-existence checks. All ✓.

### Gate Status

Gate: PASS → docs/qa/gates/5.4-workbook-azure-retheme.yml
Risk profile: docs/qa/assessments/5.4-risk-20260724.md
Test design: docs/qa/assessments/5.4-test-design-20260724.md

### Recommended Status

✓ Ready for Done
