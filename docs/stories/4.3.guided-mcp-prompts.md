# Story 4.3: Guided MCP Prompts for the Diagnostic Workflow

## Status

Done

## Story

**As a** student's AI agent,
**I want** crafted MCP prompts that guide the diagnostic workflow,
**so that** I use the tools effectively — scoping, asking follow-ups, iterating, and producing an RCA.

## Acceptance Criteria

1. The server exposes MCP **prompts** covering the workflow: scoping/triage of a ticket, formulating follow-up questions, iterative investigation across the telemetry tools, and producing an RCA that resolves the issue or documents next steps.
2. Prompts reference the actual tool surface (ticket, resource, and telemetry tools) and encourage correct multi-step, multi-round tool use.
3. Prompts are discoverable via the MCP protocol with clear names and descriptions.
4. Prompts are parameterized where useful (e.g., by ticket id) per MCP prompt conventions.
5. A documented example shows an agent using a prompt to progress from a ticket to an RCA on a representative scenario (including a multi-round one).
6. Integration tests assert the prompts are discoverable and return well-formed prompt content.

## Tasks / Subtasks

- [x] Task 1: Prompt module (AC: 1, 2, 4)
  - [x] `prompts/diagnostics.py::register_diagnostic_prompts(mcp)` with parameterized prompts: `triage_ticket(ticket_id)`, `investigate_incident(ticket_id)`, `summarize_rca(ticket_id)`
  - [x] Prompt text references the real tools (get_ticket, get_ticket_resources, query_*_logs/traces) and instructs iterative/multi-round investigation
- [x] Task 2: Register in server (AC: 3)
- [x] Task 3: Documented example (AC: 5)
  - [x] README "Guided prompts" section with an example walkthrough (single + multi-round)
- [x] Task 4: Tests (AC: 6)
  - [x] Integration: `list_prompts` includes the prompts; `get_prompt(name, {ticket_id})` returns well-formed messages referencing the ticket + tools

## Dev Notes

### Prompt API [Source: architecture/components.md#MCP-Surface]
- FastMCP: `@mcp.prompt(description=...)` on a function returning `str` → a user message. Client uses `list_prompts()` / `get_prompt(name, {args})`.
- Keep prompts self-contained and tool-aware so a connected agent gets a complete workflow with the least overhead.

### Workflow the Prompts Encode [Source: prd/epic-4-full-scenario-library-guided-prompts.md]
1. Scope: read the ticket + resources; identify product/persona/severity and the affected resource.
2. Follow-up: what to ask/clarify before diving in.
3. Investigate: pick the telemetry tool by suspected domain; if inconclusive, form a hypothesis and query another table/instance (multi-round).
4. RCA: state root cause + resolution or next steps.

### File Locations [Source: architecture/source-tree.md]
- `src/contoso_support_mcp/prompts/diagnostics.py` (new)
- `src/contoso_support_mcp/server.py` (register)
- `README.md` (guided-prompts example)
- Tests: `tests/integration/test_prompts.py` (new)

### Testing
- pytest + in-memory MCP client. Assert prompt discovery + `get_prompt` returns messages containing the ticket id and tool names.
- **Run:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-23 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-23 | 1.2 | Implemented; 3 prompts, 86/86 tests pass; status → Review | James (Dev) |
| 2026-07-23 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-23 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- `uv run pytest -q` — **86 passed** (+3 prompt integration). ruff clean (wrapped long prompt strings).

### Completion Notes List

- `prompts/diagnostics.py`: three MCP prompts — `triage_ticket`, `investigate_incident` (full scope→follow-up→iterate→RCA workflow), `summarize_rca` — all parameterized by `ticket_id` and referencing the exact tool surface.
- `investigate_incident` explicitly instructs multi-round behavior ("if inconclusive... query a DIFFERENT table or instance... some incidents require multiple rounds").
- Registered in `build_server()`; discoverable via `list_prompts`/`get_prompt`.
- README "Guided prompts" section documents a worked multi-round example (TICKET-10000026) and a single-round example (TICKET-10000001).

### File List

- `src/contoso_support_mcp/prompts/diagnostics.py` (new)
- `src/contoso_support_mcp/server.py` (modified — register prompts)
- `README.md` (modified — guided-prompts example)
- `tests/integration/test_prompts.py` (new)

## QA Results

### Review Date: 2026-07-23 — Reviewed By: Quinn (Test Architect)

**Code Quality:** The prompts are genuinely useful, not decorative — `investigate_incident` encodes the exact scope→follow-up→iterate→RCA loop the whole product is built to teach, names the real tools, and explicitly pushes the agent to a second round when the first query is inconclusive. Tests assert both tool references and the multi-round guidance text, so a future rename that breaks a prompt would fail CI.

**Compliance:** Standards ✓ | Structure ✓ | Testing ✓ | ACs ✓ (1-6)

**Traceability:** AC1→3 prompts covering the workflow; AC2→tool-reference test; AC3→discoverability test; AC4→ticket_id argument test; AC5→README example; AC6→3 integration tests. All ✓.

### Gate Status

Gate: PASS → docs/qa/gates/4.3-guided-mcp-prompts.yml
Risk profile: docs/qa/assessments/4.3-risk-20260723.md
Test design: docs/qa/assessments/4.3-test-design-20260723.md

### Recommended Status

✓ Ready for Done
