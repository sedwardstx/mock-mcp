# Story 3.2: ARM Control-Plane Trace Query Tool

## Status

Done

## Story

**As a** student's AI agent,
**I want** to query ARM control-plane traces for a resource,
**so that** I can investigate deployment, configuration, and control-plane operations related to an incident.

## Acceptance Criteria

1. A tool (e.g., `query_arm_traces`) accepts a resource identifier, an optional time range, and ARM-appropriate filters, and returns matching trace rows.
2. Results are scoped to the requested resource/time range and are deterministic for identical inputs.
3. Results are correlated to the scenario — for scenarios whose root cause is control-plane related, the traces contain the supporting evidence (with realistic surrounding noise where appropriate).
4. An empty result (no matching traces) returns an empty set, not an error; malformed inputs return a clear validation message.
5. The tool exposes a clear MCP schema documenting parameters, filters, and returned columns.
6. Integration tests assert correct, deterministic results for representative scenarios.

## Tasks / Subtasks

- [x] Task 1: Repository telemetry query (AC: 1, 2)
  - [x] `Repository.query_telemetry(table, resource_id, time_range, instance_id, filters) -> list[row]` using the shared `query_rows` semantic over all scenarios' rows for that table
- [x] Task 2: `query_arm_traces` tool (AC: 1, 4, 5)
  - [x] Params: resource_id (req), time_range (opt), activity_status (opt filter); structured `TelemetryResult` (rows as dicts, count); bad time_range → `invalid_request`; empty → `ok` empty
- [x] Task 3: Register in server (AC: 1)
- [x] Task 4: Tests (AC: 2, 3, 6)
  - [x] Integration: query ARM traces for TICKET-10000001's resource → finds the 409 AllocationFailed evidence; time-range scoping; empty for unrelated resource

## Dev Notes

### Design [Source: architecture/components.md#MCP-Surface] [Source: architecture/database-schema.md#Table-ArmControlPlaneTraces]
- Reuse `data/query.py::query_rows` (Story 3.1) — do NOT reimplement scoping.
- `Repository.query_telemetry(table_attr, ...)` gathers rows of that table across all scenarios, then applies `query_rows`. Deterministic (scenario order + fixture row order).
- Tool returns `TelemetryResult{status, resource_id, table, rows: list[dict], count, message?}`. Serialize rows via `query.as_dicts`.
- Invalid `time_range` → catch `TimeRangeError` → `invalid_request`. Empty → `ok` with `rows: []`.

### Evidence [Source: fixtures TICKET-10000001]
- TICKET-10000001 (arm root cause) has an ARM trace row with `http_status_code: 409`, `sub_status: AllocationFailed`, plus a benign 200 read (noise).

### File Locations [Source: architecture/source-tree.md]
- `src/contoso_support_mcp/data/repository.py` (extend — `query_telemetry`)
- `src/contoso_support_mcp/tools/telemetry.py` (new — ARM tool + shared result model)
- `src/contoso_support_mcp/server.py` (register)
- Tests: `tests/integration/test_telemetry_tools.py` (new)

### Testing
- pytest + in-memory MCP client. Assert the 409/AllocationFailed row is returned for the VM resource; assert time-range narrows results; assert an unrelated resource_id → empty ok.
- **Run:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-23 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-23 | 1.2 | Implemented; 65/65 tests pass, ruff clean; status → Review | James (Dev) |
| 2026-07-23 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-23 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- `uv run pytest -q` — **65 passed** (+4 ARM integration). `uv run ruff check` — clean.

### Completion Notes List

- `Repository.query_telemetry(table, resource_id, ...)` gathers a table's rows across all scenarios then applies the shared `query_rows` (deterministic).
- `tools/telemetry.py`: `TelemetryResult` model + shared `_query` helper (catches `TimeRangeError` → `invalid_request`, serializes rows via `as_dicts`) + `query_arm_traces` tool (resource_id, time_range, activity_status filter). Registered in `build_server()`.
- Verified: VM resource query returns the 409/AllocationFailed evidence (plus the benign 200 read as noise); time-range narrows to just the failure; unrelated resource → empty `ok`; bad time_range → `invalid_request`.

### File List

- `src/contoso_support_mcp/data/repository.py` (modified — `query_telemetry`)
- `src/contoso_support_mcp/tools/telemetry.py` (new — TelemetryResult, `_query`, `query_arm_traces`)
- `src/contoso_support_mcp/server.py` (modified — register telemetry tools)
- `tests/integration/test_telemetry_tools.py` (new)

## QA Results

### Review Date: 2026-07-23 — Reviewed By: Quinn (Test Architect)

**Code Quality:** Clean and DRY. The `_query` helper centralizes the error/serialize path so the remaining telemetry tools (3.3/3.4) are trivial, well-tested additions. Correlation is real: the test asserts the specific 409/AllocationFailed evidence row surfaces, not just a non-empty result. Time-range scoping verified at the boundary.

**Compliance:** Standards ✓ | Structure ✓ | Testing ✓ | ACs ✓ (1–6)

**Traceability:** AC1/3→INT-001 (evidence); AC2→INT-002 (scoping); AC4→INT-003/004 (empty/invalid); AC5→schema in description; AC6→4 integration tests. All ✓.

### Gate Status

Gate: PASS → docs/qa/gates/3.2-arm-trace-query-tool.yml
Risk profile: docs/qa/assessments/3.2-risk-20260723.md
Test design: docs/qa/assessments/3.2-test-design-20260723.md

### Recommended Status

✓ Ready for Done
