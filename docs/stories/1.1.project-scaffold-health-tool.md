# Story 1.1: Project Scaffold & Health Tool (stdio)

## Status

Review

## Story

**As an** instructor setting up the teaching server,
**I want** a Python MCP server project that starts over stdio and exposes a health/identity tool,
**so that** I have a runnable, connectable foundation and can confirm an agent is talking to the right server.

## Acceptance Criteria

1. A Python project is initialized with dependency management (e.g., pyproject/`uv` or `pip`), the official Python MCP SDK as a dependency, and a documented project structure that separates the MCP surface from the (future) mock data layer.
2. A unit + integration test harness is configured and runnable via a single command, with at least one passing smoke test.
3. The server runs over **stdio** via a single documented command and is discoverable by a standard MCP client.
4. The server exposes a health/identity tool (e.g., `get_server_info` / `ping`) that returns server name, version, and a static status, with a clear MCP schema and description.
5. An integration test connects an MCP client over stdio, invokes the health tool, and asserts the returned identity/status.
6. A brief README section documents how to install dependencies and run the server over stdio.

## Tasks / Subtasks

- [x] Task 1: Initialize Python project scaffold (AC: 1)
  - [x] Create `pyproject.toml` with project metadata, `requires-python = ">=3.11"`, entry point `contoso-support-mcp`, and dependencies (`mcp`, `pydantic`, `pyyaml`)
  - [x] Create `src/contoso_support_mcp/` package with the layout from source-tree.md (tools/, prompts/, data/, fixtures/ placeholders)
  - [x] Configure Ruff (lint+format) and pytest in `pyproject.toml`
  - [x] Generate `uv.lock` via `uv sync`
- [x] Task 2: Implement config + CLI entry point (AC: 3)
  - [x] `config.py` with a `Settings` model (transport defaulting to stdio; fixtures path)
  - [x] `__main__.py` CLI parsing `--transport` (stdio default) and dispatching to the transport runner
- [x] Task 3: Implement the MCP server surface with the health tool (AC: 3, 4)
  - [x] `server.py` builds the FastMCP app and registers tools
  - [x] `tools/health.py` implements `get_server_info` returning `{name, version, status}` via a Pydantic model with clear schema/description
  - [x] Wire stdio transport runner
- [x] Task 4: Logging discipline (AC: 3) [Critical Rule]
  - [x] Configure stdlib `logging` to **stderr only** — never stdout (stdout is the stdio transport channel)
- [x] Task 5: Test harness + smoke + integration test (AC: 2, 5)
  - [x] Configure `tests/` with unit/integration dirs and `pytest-asyncio`
  - [x] Unit smoke test asserting `get_server_info` returns expected identity
  - [x] Integration test: connect an in-memory/stdio MCP client, list tools, call the health tool, assert result
- [x] Task 6: README run docs (AC: 6)
  - [x] Document install (`uv sync`) and run over stdio (`uv run contoso-support-mcp --transport stdio`)

## Dev Notes

### Technical Constraints
- **Language/runtime:** Python 3.12 was specified in architecture; **environment has 3.11.8**, so target `requires-python = ">=3.11"`. Non-blocking deviation, recorded. [Source: architecture/tech-stack.md]
- **MCP Framework:** official `mcp` Python SDK (FastMCP) — provides stdio and (later) streamable HTTP from one codebase. Pin exact version in `uv.lock` at init. [Source: architecture/tech-stack.md]
- **Data Validation:** Pydantic 2.x for models + tool I/O schemas. [Source: architecture/tech-stack.md]
- **Package/run manager:** `uv`. [Source: architecture/tech-stack.md]

### Critical Rules (from coding-standards.md)
- **No stdout writes except the MCP transport:** never `print()`; use the logger (stderr). A stray stdout write corrupts stdio-mode framing. [Source: architecture/coding-standards.md#Critical-Rules]
- **No randomness or wall-clock at query time.** [Source: architecture/coding-standards.md#Critical-Rules]
- **Tools return structured results, never raise past the handler.** [Source: architecture/coding-standards.md#Critical-Rules]

### File Locations (from source-tree.md)
- Entry/CLI: `src/contoso_support_mcp/__main__.py`
- Config: `src/contoso_support_mcp/config.py`
- Server: `src/contoso_support_mcp/server.py`
- Health tool: `src/contoso_support_mcp/tools/health.py`
- Tests: `tests/{unit,integration}/`
[Source: architecture/source-tree.md]

### Component Responsibilities
- **Transport Layer:** `run_stdio()` for offline student mode. [Source: architecture/components.md#Transport-Layer]
- **MCP Surface:** declares/handles tools; `get_server_info` health tool. [Source: architecture/components.md#MCP-Surface]
[Source: architecture/high-level-architecture.md]

### Testing
- **Frameworks:** pytest 8.x, pytest-asyncio for async client/server flows. [Source: architecture/test-strategy-and-standards.md#Unit-Tests]
- **Location/convention:** `tests/{unit,integration}/`, files `test_*.py`. [Source: architecture/coding-standards.md#Core-Standards]
- **This story:** unit smoke test for the health tool; integration test that connects an MCP client and invokes the health tool over the in-memory/stdio transport, asserting identity/status. [Source: architecture/test-strategy-and-standards.md#Integration-Tests]
- **Run command:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-23 | 1.1 | Validated — GO (9/10, High confidence); status → Approved | Sarah (PO) |
| 2026-07-23 | 1.2 | Implemented; 4/4 tests pass, ruff clean; status → Review | James (Dev) |
| 2026-07-23 | 1.3 | QA review — Gate PASS (100/100); type-hint refactor; 4/4 pass | Quinn (QA) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- `uv sync` — resolved deps; **uv provisioned Python 3.12.13** for the venv (matches architecture's 3.12 target, so the 3.11 deviation is moot; `requires-python` remains `>=3.11`).
- `uv run pytest -v` — **4 passed** (2 unit, 2 integration).
- `uv run ruff check` — All checks passed.
- `uv run contoso-support-mcp --help` — entry point wired correctly.
- Stdout-purity check — server writes nothing to stdout on startup (logs to stderr). ✓

### Completion Notes List

- Scaffolded the `contoso_support_mcp` package per source-tree.md; placeholder packages for `tools/`, `prompts/`, `data/`, and an empty `fixtures/scenarios/`.
- `get_server_info` health tool returns name/version/status via a Pydantic `ServerInfo` model (auto JSON schema for MCP). Verified structured content over an in-memory MCP client session.
- Logging routed to stderr only; added `test_no_stdout.py` guard that fails if `print(` appears in `src/`.
- stdio transport wired via FastMCP `mcp.run(transport="stdio")`; CLI defaults to stdio.
- MCP SDK resolved to `mcp 1.28.1`; pinned in `uv.lock`.

### File List

- `pyproject.toml` (new)
- `uv.lock` (new, generated)
- `README.md` (replaced stub)
- `src/contoso_support_mcp/__init__.py` (new)
- `src/contoso_support_mcp/__main__.py` (new)
- `src/contoso_support_mcp/config.py` (new)
- `src/contoso_support_mcp/server.py` (new)
- `src/contoso_support_mcp/tools/__init__.py` (new)
- `src/contoso_support_mcp/tools/health.py` (new)
- `src/contoso_support_mcp/prompts/__init__.py` (new)
- `src/contoso_support_mcp/data/__init__.py` (new)
- `src/contoso_support_mcp/fixtures/scenarios/.gitkeep` (new)
- `tests/__init__.py`, `tests/unit/__init__.py`, `tests/integration/__init__.py` (new)
- `tests/unit/test_health.py` (new)
- `tests/unit/test_no_stdout.py` (new)
- `tests/integration/test_server_stdio.py` (new)

## QA Results

### Review Date: 2026-07-23

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

High quality for a foundational story. Clean layering (MCP surface vs. future data layer), a pure `server_info()` extracted for cheap unit testing, disciplined stderr-only logging, and a proactive `test_no_stdout.py` guard against the classic stdio-corruption pitfall. Deterministic and reproducible (`uv.lock` committed).

### Refactoring Performed

- **File**: `src/contoso_support_mcp/tools/health.py`
  - **Change**: Added `FastMCP` type hint to the `register_health_tools(mcp)` parameter (and its import).
  - **Why**: Improves readability and type safety; makes the registration contract explicit as more tool modules adopt the same pattern.
  - **How**: Import `from mcp.server.fastmcp import FastMCP`; annotate the parameter. Re-ran tests (4/4 pass) and ruff (clean) to confirm no regression.

### Compliance Check

- Coding Standards: ✓ (stderr-only logging; no stdout in `src/`; structured tool result via Pydantic model)
- Project Structure: ✓ (matches `architecture/source-tree.md`)
- Testing Strategy: ✓ (unit + integration via in-memory MCP client, per `test-strategy-and-standards.md`)
- All ACs Met: ✓ (1–6 traced to passing tests)

### Requirements Traceability

| AC | Validated by | Result |
|----|--------------|--------|
| 1 Scaffold + deps + structure | `uv sync` + package import (1.1-INT-000) | ✓ |
| 2 Test harness + smoke | `test_health.py` (1.1-UNIT-001) | ✓ |
| 3 stdio run + discoverable | `test_server_stdio.py::discoverable` (1.1-INT-001) + CLI `--help` | ✓ |
| 4 Health tool schema/content | `ServerInfo` model + 1.1-UNIT-002 | ✓ |
| 5 Integration invokes health | `test_server_stdio.py::returns_identity` (1.1-INT-002) | ✓ |
| 6 README run docs | README install + stdio section (1.1-DOC-001) | ✓ |

### Improvements Checklist

- [x] Added FastMCP type hint to `register_health_tools` (health.py)
- [x] Verified stdout purity guard is in place and green
- [ ] (Future) Confirm streamable-HTTP transport API when Story 1.2 adds network mode

### Security Review

No concerns. No real data, secrets, auth, or external calls in this story. The stdout-discipline guard protects protocol integrity.

### Performance Considerations

None. In-memory identity call; full suite runs <1s.

### Files Modified During Review

- `src/contoso_support_mcp/tools/health.py` (type-hint refactor) — Dev File List already covers this file; no new files added.

### Gate Status

Gate: PASS → docs/qa/gates/1.1-project-scaffold-health-tool.yml
Risk profile: docs/qa/assessments/1.1-risk-20260723.md
Test design: docs/qa/assessments/1.1-test-design-20260723.md

### Recommended Status

✓ Ready for Done
