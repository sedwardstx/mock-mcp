# Story 2.5: Seed Scenario Batch & Consistency Validation

## Status

Done

## Story

**As an** instructor,
**I want** a first batch of ~20–30 realistic, internally consistent scenarios,
**so that** early labs have believable ticket content spanning both customer personas and several Azure products.

## Acceptance Criteria

1. The dataset contains ~20–30 distinct scenarios authored as tickets, covering a spread of Azure products/platforms and both customer personas (Windows admins and Azure developers).
2. Each scenario includes a coherent symptom/problem statement, affected resource(s), severity/status, and a defined root cause + resolution/next-step (telemetry to be added in Epic 3).
3. Ticket ids, resources, and personas are internally consistent, and all scenarios pass the loader's schema validation.
4. An automated consistency check verifies, for every scenario, that required fields are present and that ticket↔resource references resolve correctly; it runs in the test suite and fails on any inconsistency.
5. The ticket list/search/get and resource-pivot tools return correct results across the full seed batch, verified by tests.

## Tasks / Subtasks

- [x] Task 1: Author the seed batch to ~20 scenarios (AC: 1, 2)
  - [x] Curated authoring covering ARM/Network/Compute-host/Compute-guest root causes, both personas, VM + VMSS resources; telemetry empty (Epic 3)
- [x] Task 2: Consistency validator (AC: 3, 4)
  - [x] `data/consistency.py::check_dataset(dataset) -> list[str]` returns issues (empty = consistent): every `ticket.resource_ids` resolves to a `resources[].resource_id`; scenario_id == ticket_id; root_cause present
- [x] Task 3: Consistency test over the whole library (AC: 4)
  - [x] `tests/consistency/test_scenario_consistency.py` loads shipped fixtures and asserts `check_dataset` returns no issues
- [x] Task 4: Coverage + tools-across-batch tests (AC: 1, 5)
  - [x] Assert scenario count in range and both personas present; list/search/get/pivot behave across the full batch

## Dev Notes

### Authoring [Source: prd/epic-2-mock-data-model-ticket-tools.md] [Source: architecture/database-schema.md#Scenario-Fixture-File]
- Reach ~20 total scenarios (3 already exist). Spread root causes across `arm`/`network`/`compute_host`/`compute_guest`; both personas; VM and VMSS resource types.
- Telemetry stays empty (`telemetry: {}`) — Epic 3 backfills. `difficulty: single_round` for this batch; multi-round arrives in Epic 3.
- Each scenario_id mirrors its ticket_id; unique ids; resource ids are valid ARM ids consistent with `ticket.resource_ids`.

### Consistency Rules [Source: architecture/test-strategy-and-standards.md#Consistency-Tests]
- Every `ticket.resource_ids` entry must appear in that scenario's `resources[].resource_id`.
- `scenario_id == ticket.ticket_id`.
- `root_cause.summary`/`resolution` non-empty.
- (Telemetry-evidence consistency is added in Epic 3.)

### File Locations [Source: architecture/source-tree.md]
- `src/contoso_support_mcp/fixtures/scenarios/*.yaml` (new files)
- `src/contoso_support_mcp/data/consistency.py` (new)
- `scripts/author_seed_scenarios.py` (authoring aid — emits curated YAML deterministically)
- Tests: `tests/consistency/test_scenario_consistency.py`, extend coverage tests

### Testing
- pytest. Consistency suite iterates ALL shipped fixtures. Coverage test asserts count in [20, 30] and both personas present. Reuse the existing tool integration tests to confirm behavior across the batch.
- **Run:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-23 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-23 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-23 | 1.2 | Implemented; 20 scenarios; 53/53 tests pass; status → Review | James (Dev) |
| 2026-07-23 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-23 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- Authored 17 new scenarios via `scripts/author_seed_scenarios.py` (curated content → deterministic YAML) → **20 total**.
- Load verification: 20 scenarios; categories arm=5, network=6, compute_guest=5, compute_host=4; personas windows_admin=11, azure_developer=9.
- `uv run pytest -q` — **53 passed** (+6 new; 3 earlier count-based search tests generalized for the larger batch).
- `uv run ruff check` — clean (added `scripts/*` E501 ignore for curated prose).

### Completion Notes List

- Seed batch reaches 20 scenarios spanning ARM/Network/Compute-host/Compute-guest root causes, both personas, and VM + VMSS resources (products: Azure VMs, VMSS, Networking). Telemetry empty (`{}`); Epic 3 backfills. All `single_round`.
- `data/consistency.py::check_dataset` returns issue strings (empty == consistent): validates scenario_id==ticket_id, every ticket resource_id resolves in resources[], non-empty root_cause summary/resolution.
- Consistency suite (`tests/consistency/`) asserts the whole shipped library is consistent + a negative case proving the validator catches a broken reference; coverage test asserts size/persona/product/category spread.
- **Note:** 3 earlier search tests hard-coded counts from the 3-sample era; generalized to assert AND-semantics/subset properties so they stay correct as the library grows.

### File List

- `scripts/author_seed_scenarios.py` (new — authoring aid)
- `src/contoso_support_mcp/fixtures/scenarios/TICKET-10000004.yaml` … `TICKET-10000020.yaml` (17 new)
- `src/contoso_support_mcp/data/consistency.py` (new)
- `tests/consistency/__init__.py`, `tests/consistency/test_scenario_consistency.py` (new)
- `tests/integration/test_batch.py` (new)
- `tests/unit/test_repository.py`, `tests/integration/test_ticket_tools.py` (modified — generalized assertions)
- `pyproject.toml` (modified — scripts E501 ignore)

## QA Results

### Review Date: 2026-07-23 — Reviewed By: Quinn (Test Architect)

**Code Quality:** Strong. The consistency validator is the right control for a growing library and is proven by both a positive (whole-library) and negative (broken-reference) test. Generating the seed via a curated script keeps content deterministic while giving each scenario distinct symptom/root-cause text. Dev correctly generalized the earlier count-based assertions instead of pinning them — good instinct that prevents brittle tests as Epic 3/4 grow the library.

**Compliance:** Coding Standards ✓ | Project Structure ✓ | Testing Strategy ✓ (consistency suite established) | All ACs ✓

**Traceability:** AC1→batch-size + coverage tests; AC2→curated content; AC3→CON-002 (load) + coverage; AC4→CON-001 + validator negative test; AC5→test_batch integration. All ✓.

**Coverage note:** 20 scenarios; all 4 root-cause categories and both personas present. Slight persona skew (11/9) is acceptable; Epic 4 rebalances toward 100+.

### Gate Status

Gate: PASS → docs/qa/gates/2.5-seed-scenario-batch-consistency.yml
Risk profile: docs/qa/assessments/2.5-risk-20260723.md
Test design: docs/qa/assessments/2.5-test-design-20260723.md

### Recommended Status

✓ Ready for Done

## QA Results

_Pending QA review._
