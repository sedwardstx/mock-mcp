# Story 5.2: Day-1 Workshop Starter Scaffold

## Status

Done

## Story

**As an** instructor,
**I want** the Azure-themed Copilot starter files (instructions, prompts, a log-triage skill, sample data, and MCP config) in this repo,
**so that** participants can run the Day-1 labs and connect to this server without a separate starter repo.

## Acceptance Criteria

1. `.github/copilot-instructions.md` (Azure support context: Compute/Networking/ARM control-plane; cite-evidence; no PII; escalation) and a scoped `.github/instructions/*.instructions.md` example are present.
2. Two reusable prompt files (`summarize-ticket`, `draft-customer-reply`) with correct frontmatter are present and Azure-worded.
3. A `.github/skills/log-triage/` skill (SKILL.md + `parse_logs.py` + `error-codes.md`) triages Azure logs; the helper ranks ERROR/FATAL lines by an `AZURE-####` code.
4. `samples/logs/*.log`, `samples/tickets/*.md`, and a `samples/queries/*.kql` exist with realistic Azure content; sample ticket ids match server fixtures; log codes match the skill's reference table and regex.
5. `.vscode/mcp.json` (top-level `servers`) wires this server for stdio (with a commented HTTP + inputs example); it does not disturb `.vscode/settings.json`.

## Tasks / Subtasks

- [x] Task 1: Sample data first (AC: 4)
  - [x] `samples/logs/arm-allocation-failure.log`, `nsg-deny-1433.log`, `vm-host-degraded.log` — each ERROR line carries an `AZURE-####` code + real sub-status, with a WARN trigger within 60s
  - [x] `samples/tickets/TICKET-10000001.md`, `TICKET-10000002.md` (ids/resources match fixtures; no PII)
  - [x] `samples/queries/allocation-failures.kql`
- [x] Task 2: Instructions (AC: 1)
  - [x] `.github/copilot-instructions.md` (applyTo `**`), `.github/instructions/kql.instructions.md` (applyTo `**/*.kql`)
- [x] Task 3: Prompts (AC: 2)
  - [x] `.github/prompts/summarize-ticket.prompt.md`, `draft-customer-reply.prompt.md`
- [x] Task 4: Skill (AC: 3)
  - [x] `.github/skills/log-triage/SKILL.md`, `parse_logs.py` (regex `AZURE-\d{4}`), `error-codes.md` (codes match samples)
- [x] Task 5: MCP config (AC: 5)
  - [x] `.vscode/mcp.json` (`servers.contoso-support` stdio via `${workspaceFolder}`; commented HTTP + inputs) — do NOT touch `settings.json`
- [x] Task 6: Verify
  - [x] `parse_logs.py` runs on each sample log and reports an `AZURE-####` code (not UNCODED); `.vscode/mcp.json` is valid JSONC; existing `uv run pytest`/`ruff` still green

## Dev Notes

### Conventions [Source: docs/external/Participant_Workbook.md] (Day-1 Labs 1–3, Appendix A)
- Follow the workbook's exact frontmatter for instructions/prompts/skills, re-themed to Azure.
- `.vscode/mcp.json` uses top-level `servers` key (VS Code). Reuse the stdio block shape from `README.md` but with `${workspaceFolder}`. [Source: architecture/workshop-integration-epic-5.md]

### Cross-file consistency (MUST hold) [Source: architecture/workshop-integration-epic-5.md]
- The `AZURE-####` codes are identical across `samples/logs/*`, `skills/log-triage/error-codes.md`, and `parse_logs.py`'s regex. Use `AZURE-1001` (ARM/AllocationFailed), `AZURE-2003` (Network/NSG Deny 1433), `AZURE-3005` (Compute host Degraded), plus a guest code `AZURE-4008` (SCM 7031).
- Sample ticket ids/resources match server fixtures (`TICKET-10000001` prod-web-01; `TICKET-10000002` api-vmss). [Source: fixtures/scenarios]
- No secrets, no PII (NFR11).

### Server tools to reference [Source: architecture/components.md#MCP-Surface]
- `get_ticket`, `get_ticket_resources`, `search_tickets`, `search_known_issues`, `query_arm_traces`, `query_network_logs`, `query_compute_host_logs`, `query_compute_guest_logs`.

### File Locations [Source: architecture/workshop-integration-epic-5.md]
- `.github/copilot-instructions.md`, `.github/instructions/kql.instructions.md`, `.github/prompts/*.prompt.md`, `.github/skills/log-triage/*`, `samples/{logs,tickets,queries}/*`, `.vscode/mcp.json`.

### Testing
- These are docs/config, not exercised by the Python suite. Verify: run `parse_logs.py` on each sample log (reports a code); confirm `.vscode/mcp.json` parses; keep the existing `uv run pytest` + `uv run ruff check` green (no server code changes here).
- **Run:** `python .github/skills/log-triage/parse_logs.py samples/logs/arm-allocation-failure.log`; `uv run pytest -q`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-24 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-24 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-24 | 1.2 | Implemented; scaffold verified, 100/100 regression; status → Review | James (Dev) |
| 2026-07-24 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-24 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- `parse_logs.py` on all 4 sample logs → each reports its `AZURE-####` code (1001/2003/3005/4008), none UNCODED.
- Log codes == error-codes.md codes (exact set). `.vscode/mcp.json` valid JSONC; `settings.json` untouched.
- Sample ticket ids TICKET-10000001/2 resolve in the loaded dataset. Regression: `uv run pytest -q` **100 passed**, ruff clean (no server code changed).

### Completion Notes List

- Sample data: 4 Azure logs (added a 4th, `vm-guest-service-crash.log`, so all four `AZURE-####` reference codes have a matching sample — arm/net/host/guest), 2 sample tickets matching server fixtures, 1 KQL demo file.
- Instructions: `.github/copilot-instructions.md` (Azure domains + domain→tool map + escalation) and `.github/instructions/kql.instructions.md` (`applyTo **/*.kql`).
- Prompts: `summarize-ticket`, `draft-customer-reply` (Azure-worded, correct frontmatter).
- Skill: `.github/skills/log-triage/` (SKILL.md + `parse_logs.py` regex `AZURE-\d{4}` + `error-codes.md`).
- `.vscode/mcp.json` (JSONC) wires `contoso-support` stdio via `${workspaceFolder}`, with commented HTTP + inputs teaching examples; `settings.json` left intact.
- `parse_logs.py` uses `print()` — allowed (it's a workshop helper under `.github/`, not server `src/`; the no-stdout guard only scans `src/`).

### File List

- `samples/logs/{arm-allocation-failure,nsg-deny-1433,vm-host-degraded,vm-guest-service-crash}.log` (new)
- `samples/tickets/{TICKET-10000001,TICKET-10000002}.md` (new)
- `samples/queries/allocation-failures.kql` (new)
- `.github/copilot-instructions.md`, `.github/instructions/kql.instructions.md` (new)
- `.github/prompts/{summarize-ticket,draft-customer-reply}.prompt.md` (new)
- `.github/skills/log-triage/{SKILL.md,parse_logs.py,error-codes.md}` (new)
- `.vscode/mcp.json` (new)

## QA Results

### Review Date: 2026-07-24 — Reviewed By: Quinn (Test Architect)

**Code Quality:** The cross-file consistency that makes or breaks this story is verified: the `AZURE-####` code set is identical across sample logs, `error-codes.md`, and the parser regex, and the parser reports a code for every sample. Sample tickets reference real server fixtures (verified resolvable), so the Day-1 files and Day-2 MCP corroborate each other. `.vscode/mcp.json` is valid JSONC and correctly leaves `settings.json` alone. Adding the 4th (guest) sample log to cover `AZURE-4008` was the right call — every reference code now has a demo.

**Compliance:** Standards ✓ | Structure ✓ (matches workshop-integration-epic-5) | Testing ✓ (verification-level, appropriate for docs/config) | ACs ✓ (1–5) | NFR11 ✓ (no secrets/PII).

**Traceability:** AC1→instructions present; AC2→2 prompts w/ frontmatter; AC3→skill + parser code output; AC4→code-consistency + ticket-resolve checks; AC5→JSONC validity + settings untouched. All ✓.

### Gate Status

Gate: PASS → docs/qa/gates/5.2-day1-workshop-starter-scaffold.yml
Risk profile: docs/qa/assessments/5.2-risk-20260724.md
Test design: docs/qa/assessments/5.2-test-design-20260724.md

### Recommended Status

✓ Ready for Done
