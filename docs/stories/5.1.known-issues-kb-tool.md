# Story 5.1: Known-Issues KB Tool

## Status

Done

## Story

**As a** student's AI agent,
**I want** to search a read-only Azure known-issues knowledge base,
**so that** I can look up generic remediation guidance for a symptom without it revealing the ticket-specific answer.

## Acceptance Criteria

1. A `search_known_issues` tool accepts optional `query` (keyword), `product`, and `category` filters (AND semantics) and returns matching known-issue entries (title, product, category, symptom, remediation, optional doc link).
2. The KB is a **separate curated dataset** (≥ 12 generic entries across ARM/Network/Compute-host/Compute-guest and the three products) that does NOT copy any per-scenario root cause/resolution — it cannot leak the grading key.
3. An invalid `category` returns a structured `invalid_request`; no matches returns `ok` with an empty list; results are deterministic.
4. The tool exposes a clear MCP schema/description stating it is generic guidance, not ticket-specific RCA.
5. Unit + integration tests cover loading/validation, each filter, keyword search, no-match, and invalid category; the KB loads at startup via the same fail-fast pattern as scenarios.

## Tasks / Subtasks

- [x] Task 1: KB data + model (AC: 2)
  - [x] `fixtures/known_issues.yaml` — top-level `known_issues:` list, 13 generic entries (arm×4, network×3, compute_host×3, compute_guest×3) across the 3 products; each `{id: KB-<DOMAIN>-###, title, product, category, symptom, remediation, doc_link}`. No per-ticket resolution text.
  - [x] `data/models.py` — add `KnownIssue` (strict; `category: RootCauseCategory`; `doc_link: str | None`); `KNOWN_ISSUE_ID_RE`.
- [x] Task 2: Loader + config (AC: 5)
  - [x] `config.py` — `DEFAULT_KNOWN_ISSUES_PATH`.
  - [x] `data/loader.py` — `load_known_issues(path) -> list[KnownIssue]` (fail-fast, unique-id, id-sorted).
- [x] Task 3: Repository + tool (AC: 1, 3, 4)
  - [x] `data/repository.py` — `Repository(dataset, known_issues=None)`; `search_known_issues(*, query, product, category)` (AND; case-insensitive substring over title+symptom+remediation).
  - [x] `tools/kb.py` — `register_kb_tools(mcp, repo)`; `search_known_issues(query=None, product=None, category=None)`; validate category → `invalid_request`; empty → `ok`.
  - [x] `server.py` — load KB + register; startup log includes KB count.
- [x] Task 4: Tests (AC: 5)
  - [x] `tests/unit/test_known_issues.py` (loader + repository + model negatives).
  - [x] `tests/integration/test_kb_tools.py` (no-arg, category filter, product filter, keyword hit, no-match, invalid category).

## Dev Notes

### Design [Source: architecture/data-models.md#KnownIssue-KB-Epic-5] [Source: architecture/workshop-integration-epic-5.md]
- KB is a curated dataset **decoupled** from `scenario.root_cause` — must NOT copy `resolution` text (grading key is `docs/scenario-index.md`). [Source: prd/epic-5-workshop-integration.md] (Story 5.1 AC2)
- Mirror existing patterns: loader like `data/loader.py::load_scenarios`; tool like `tools/tickets.py`; result model with `status ∈ {ok, invalid_request}` (no `not_found` — a search that matches nothing is `ok` + empty list). [Source: architecture/error-handling-strategy.md#Business-Logic-Errors]
- `category` validated against `RootCauseCategory` (arm/network/compute_host/compute_guest); `product` is free-text (unknown product → empty ok). [Source: architecture/data-models.md]
- Repository constructor gains an optional `known_issues=None` arg so all existing call-sites/tests keep working. Deterministic (id-sorted source list). [Source: architecture/components.md#Repository]

### File Locations [Source: architecture/source-tree.md] [Source: architecture/workshop-integration-epic-5.md]
- `src/contoso_support_mcp/fixtures/known_issues.yaml` (sibling of `scenarios/`; scenario glob `scenarios/*.yaml` ignores it)
- `src/contoso_support_mcp/data/{models.py,loader.py,repository.py}`, `config.py`, `tools/kb.py`, `server.py`
- Tests: `tests/unit/test_known_issues.py`, `tests/integration/test_kb_tools.py`

### Testing
- pytest + in-memory MCP client (as in `tests/integration/test_ticket_tools.py`). Assert `search_known_issues({"query":"allocation"})` hits `KB-ARM-001`; `{"category":"martian"}` → invalid_request; `{"query":"zzz"}` → ok empty.
- Grep tests for any hardcoded tool count (surface 9→10).
- **Run:** `uv run pytest`.

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-07-24 | 1.0 | Initial draft | Bob (SM) |
| 2026-07-24 | 1.1 | Validated — GO (9/10); status → Approved | Sarah (PO) |
| 2026-07-24 | 1.2 | Implemented; 100/100 tests pass, ruff clean; status → Review | James (Dev) |
| 2026-07-24 | 1.3 | QA review — Gate PASS (100/100) | Quinn (QA) |
| 2026-07-24 | 1.4 | Closed — gate PASS, all ACs met; status → Done | Sarah (PO) |

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Dev agent: James)

### Debug Log References

- `uv run pytest -q` — **100 passed** (+11: 6 unit, 5 integration). `uv run ruff check` — clean.
- Manual: tool count now **10**; `search_known_issues({"query":"allocation"})` → `KB-ARM-001`; `{"category":"martian"}` → `invalid_request`; scenario count unchanged at 103.

### Completion Notes List

- `KnownIssue` strict model (own `KB-<DOMAIN>-###` id validator; `category` reuses `RootCauseCategory`); `load_known_issues` fail-fast + unique-id + id-sorted (deterministic); `DEFAULT_KNOWN_ISSUES_PATH`.
- `Repository.__init__` gained an optional `known_issues=None` (all existing call-sites/tests untouched) + `search_known_issues` (AND filters, case-insensitive keyword).
- `tools/kb.py::search_known_issues` — structured `ok`/`invalid_request`; empty match is `ok`. Registered in `build_server()`; startup log includes KB count.
- **Anti-leak (AC2):** `known_issues.yaml` is 13 curated GENERIC entries; no text copied from any scenario `root_cause.resolution`. KB file is a sibling of `scenarios/` so the scenario loader's `scenarios/*.yaml` glob ignores it (verified: scenario count still 103).

### File List

- `src/contoso_support_mcp/fixtures/known_issues.yaml` (new)
- `src/contoso_support_mcp/data/models.py` (modified — `KnownIssue`, `KNOWN_ISSUE_ID_RE`)
- `src/contoso_support_mcp/config.py` (modified — `DEFAULT_KNOWN_ISSUES_PATH`)
- `src/contoso_support_mcp/data/loader.py` (modified — `load_known_issues`)
- `src/contoso_support_mcp/data/repository.py` (modified — ctor arg + `search_known_issues`)
- `src/contoso_support_mcp/tools/kb.py` (new)
- `src/contoso_support_mcp/server.py` (modified — load + register KB)
- `tests/unit/test_known_issues.py` (new), `tests/integration/test_kb_tools.py` (new)

## QA Results

### Review Date: 2026-07-24 — Reviewed By: Quinn (Test Architect)

**Code Quality:** Clean, pattern-consistent addition. The Repository constructor change is backward-compatible (`known_issues=None`), so the entire prior suite stays green. The anti-leak requirement — the one that actually matters here — is verified two ways: (1) the KB file contains **zero** ticket ids / resource ids (grep), and (2) the shipped remediations are generic-by-category, independent of any scenario `resolution`. The scenario count is unchanged at 103, confirming the KB file isn't picked up by the scenario glob.

**Compliance:** Standards ✓ | Structure ✓ | Testing ✓ | ACs ✓ (1–5)

**Traceability:** AC1→repository/tool filter tests; AC2→13 curated entries + no-leak grep + scenario-count test; AC3→invalid-category + no-match tests; AC4→tool description asserts "generic guidance"; AC5→loader/negative + integration tests. All ✓.

**Security:** No ticket/resource/PII in the KB; no credentials. No concerns.

### Gate Status

Gate: PASS → docs/qa/gates/5.1-known-issues-kb-tool.yml
Risk profile: docs/qa/assessments/5.1-risk-20260724.md
Test design: docs/qa/assessments/5.1-test-design-20260724.md

### Recommended Status

✓ Ready for Done
